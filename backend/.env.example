# Backend environment example - DO NOT COMMIT SECRETS.
# Set SUPABASE_URL to your Supabase project URL and SUPABASE_KEY to a server-side key
# (service_role) in your deployment environment (e.g. Render/Heroku). Do NOT store
# service_role in the frontend or commit it to git.
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_KEY="your-service-role-key"

# FRONTEND_URL is used for CORS allowed origin in development / deployments.
FRONTEND_URL="http://localhost:5173"
